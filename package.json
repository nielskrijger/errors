{
  "name": "@nielskrijger/errors",
  "version": "0.1.0",
  "description": "Basic express error handler",
  "main": "dist/index.js",
  "author": "Niels Krijger <niels@kryger.nl>",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0"
  },
  "scripts": {
    "build": "rm -rf ./dist && babel src --out-dir dist",
    "prepublish": "npm run build"
  }
}
